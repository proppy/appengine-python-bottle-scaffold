# This requirements file lists all third-party dependencies for this project.
#
# Run 'pip install -r requirements.txt -t lib/' to install these dependencies
# in this project's `lib` directory.
# (the `lib` directory is added to the `sys.path` by `appengine_config.py`).

bottle==0.11.6

# Note that App Engine requires that project dependencies be installed
# inside the project directory in order to be deployed with your
# application.
